import SectionTitle from "../../../../components/SectionTitle";

const MenuSection = () => {
    return (
        <div>
            <SectionTitle heading={'FROM OUR MENU'} message={'Check it out'} />
        </div>
    );
};

export default MenuSection;